<h3>一般アカウント一覧</h3>

  <table>
    <thead>
      <tr>
        <td>ID</td>
        <td>氏名</td>
        <td>所属</td>
        <td>メールアドレス</td>
        <td></td>
      </tr>
    </thead>
    <% @users.each do |user| %>
      <tbody>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.name%></td>
          <td><%= user.department %></td>
          <td><%= user.email %></td>
          <td><%= link_to "削除", admin_user_path(user), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
        </tr>
      </ybody>
    <% end %>
  </table>
