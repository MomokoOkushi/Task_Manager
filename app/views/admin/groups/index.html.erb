<h3>グループ一覧</h3>

<% @groups.each do |group| %>
  <table>
    <tr>
      <td><%= link_to group.name, admin_group_path(group) %></td>
      <td><%= link_to "削除", admin_group_path(group), method: :delete %></td>
    </tr>
  </table>
<% end %>